# application layer

放用户动作对应的用例服务，例如：

- Agent Orchestrator：决定下一步、阶段切换、Agent 选择、失败后策略
- 创建课堂
- 创建视频上传任务
- 上传完成后启动处理流程
- 保存课堂记录校订
- 重建课堂分段
- 生成报告

这一层可以调用 domain 规则、数据库仓储、对象存储接口和外部 AI 适配器，但不关心 HTTP 细节。
